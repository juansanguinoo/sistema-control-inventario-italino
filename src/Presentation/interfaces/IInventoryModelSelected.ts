export interface IInventoryModelSelected {
  inventoryId: number;
  nameInventory: string;
  imageInventory: string;
  sellingPriceInventory: number;
  stockInventory: number;
  quantity: number;
  subTotal: number;
}
